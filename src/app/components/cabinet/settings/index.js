import React, { Component } from 'react';
import './settings.css';

class UserSettings extends Component {
    render() {
        return (
            <div>
                isSettings
            </div>
        );
    }
}

export default UserSettings;